export function Footer() {
    return (
        <div className="panel-footer">
            <button id="add-cookie-btn" className="primary-footer-btn" title="Add New Cookie" type="button">
                <i className="fas fa-plus" aria-hidden="true" /> Add New Cookie
            </button>
        </div>
    );
}
